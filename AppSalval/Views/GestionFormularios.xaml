<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppSalval.Views.GestionFormularios"
             Title="GestionFormularios">
    <VerticalStackLayout>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            
            <!-- Botón para desplegar opciones -->
            <Button Text="Opciones"
                Grid.Row="0"
                Grid.Column="0"
                HorizontalOptions="Start"
                VerticalOptions="Start"
                    Margin="20,10,0,0">
                <!--Clicked="OnOpcionesClicked"-->
                </Button>
            <Label
                Grid.Row="0"
                Grid.Column="0"
                Text="Administrador" 
                FontSize="Title"
                HorizontalOptions="Center"
                VerticalOptions="Start"
                TextColor="MidnightBlue"
                 Margin="30,20,0,0"/>
            <Button Text="Menu"
                Grid.Row="0"
                Grid.Column="0"
                HorizontalOptions="End"
                VerticalOptions="Start"
                    Margin="0,10,20,0">
                <!--Clicked="OnOpcionesClicked"-->
            </Button>

            <!-- Contenido principal de la página -->
            <ContentView Grid.Row="2" Grid.ColumnSpan="2" VerticalOptions="Center">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Label
                        Text="Gestión de formularios"
                        FontSize="Medium"
                        TextColor="MidnightBlue"
                        HorizontalOptions="Center"
                        VerticalOptions="Center"
                        Margin="0,0,0,0"
                        Grid.Column="0" 
                        Grid.Row="0"
                        />
                        
                    
                    <Button
                        Text="Añadir"
                        FontSize="Medium"
                        TextColor="White"
                        BackgroundColor="#1edb68"
                        HorizontalOptions="End"
                        VerticalOptions="Center"
                        Margin="0,20,30,0"
                        Grid.Column="1" />
                </Grid>
                
                
            </ContentView>
            <CollectionView Grid.Row="3" Grid.ColumnSpan="3" ItemsSource="{Binding Formularios}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10" ColumnDefinitions="*,Auto,Auto,Auto">
                            <Label Text="{Binding Nombre}" FontSize="16" VerticalOptions="Center" />
                            <Button Command="{Binding VerCommand}" WidthRequest="24" HeightRequest="24" />
                            <Button Command="{Binding EditarCommand}" WidthRequest="24" HeightRequest="24" />
                            <Button Command="{Binding EliminarCommand}" WidthRequest="24" HeightRequest="24" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </VerticalStackLayout>
</ContentPage>